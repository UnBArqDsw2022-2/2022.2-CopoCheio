import MainButton from "../components/atoms/MainButton";

const Home = () => (
    <MainButton
        type="primary"
        onClick={() => console.log()}
    >
        Cala a boca
    </MainButton>
);

export default Home;